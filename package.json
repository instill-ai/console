{
  "name": "instillai-console",
  "version": "0.0.1",
  "main": "index.js",
  "repository": "https://github.com/instill-ai/console.git",
  "author": "Instill AI",
  "license": "Apache License 2.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "app"
  ],
  "scripts": {
    "docker:web-app:install": "docker buildx bake -f ./docker-compose.app.yml --progress=tty deps",
    "docker:app:develop": "cross-env DOCKER_BUILDKIT=1 docker-compose -f docker-compose.yml -f ./docker-compose.app.yml up --build develop"
  },
  "dependencies": {
    "cross-env": "^7.0.3"
  }
}